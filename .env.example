# Real env vars per docs/ARCHITECTURE.md §6. Values never committed.
# Stage A needs none of these (local-first). Stage B requires all.

DATABASE_URL=postgresql://user:password@localhost:5432/montara
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3000
BILLING_ENABLED=false

# Stage B mail (recovery + pilot comms). Provider TBD.
MAIL_HOST=
MAIL_PORT=
MAIL_USER=
MAIL_PASS=
MAIL_FROM=
